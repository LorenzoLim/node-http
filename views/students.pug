html
  head
    title Hello!
  body
    - let name = "Scott"
    h1= students
    h1 Student list:
    ul
      each student in students
        li= student
    form(action='/students', method='POST')
      p Add a new student!
      input(name='student_name')
      input(type='submit')
